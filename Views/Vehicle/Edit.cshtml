@model GestorViajes.Models.ViewModels.Vehicle.VehicleViewModel

@{
    ViewData["Title"] = "Editar Vehículo";
}

<h2 class="mb-4">Editar Vehículo</h2>

@if (TempData["message"] != null)
{
    <div class="alert alert-@(TempData["status"])">
        @TempData["message"]
    </div>
}

<div asp-validation-summary="ModelOnly" class="text-danger"></div>

<form asp-action="Edit" asp-controller="Vehicle" method="post">
    @Html.AntiForgeryToken()
    <input type="hidden" asp-for="Id" />

    <!-- Matrícula -->
    <div class="mb-3">
        <label class="form-label fw-bold">Matrícula</label>
        <input type="text" class="form-control" asp-for="Plate" required />
        <span asp-validation-for="Plate" class="text-danger"></span>
    </div>

    <!-- Modelo -->
    <div class="mb-3">
        <label class="form-label fw-bold">Modelo</label>
        <input type="text" class="form-control" asp-for="Model" required />
        <span asp-validation-for="Model" class="text-danger"></span>
    </div>

    <!-- Color -->
    <div class="mb-3">
        <label class="form-label fw-bold">Color</label>
        <input type="text" class="form-control" asp-for="Color" required />
        <span asp-validation-for="Color" class="text-danger"></span>
    </div>

    <!-- Número de Plazas -->
    <div class="mb-3">
        <label class="form-label fw-bold">Número de Plazas</label>
        <input asp-for="MaxSeats" type="number" class="form-control" min="1" max="3" required />
        <span asp-validation-for="MaxSeats" class="text-danger"></span>
        <small class="form-text text-muted">Máximo 3 plazas.</small>
    </div>

    <!-- Botones -->
    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-primary">Guardar Cambios</button>
        <a asp-action="Index" asp-controller="Vehicle" class="btn btn-secondary">Atrás</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
